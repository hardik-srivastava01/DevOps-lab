# Linux Day 3 Practice Log

## Objective

Practice Linux file system navigation, directory listing, file searching, and command history using basic Linux commands.

---

## Commands Executed

```bash
pwd
ls
ls -a
ls -lh
ls -R
cd
find
which
history
```

---

## Result

- Checked the current working directory using `pwd`.
- Listed files and directories using `ls`.
- Displayed hidden files using `ls -a`.
- Viewed file sizes in a human-readable format using `ls -lh`.
- Listed all files and subdirectories recursively using `ls -R`.
- Navigated between directories using `cd`.
- Searched for files and directories using `find`.
- Located the path of Linux commands using `which`.
- Displayed previously executed commands using `history`.

---

## Skills Practiced

- Directory navigation (`pwd`, `cd`)
- File and directory listing (`ls`, `ls -a`, `ls -lh`, `ls -R`)
- File searching (`find`)
- Command location (`which`)
- Command history (`history`)

---

## Issues Faced

Initially, I was confused about the difference between the `find` and `which` commands.

- `find` is used to search for files and directories.
- `which` is used to locate the executable path of a command.

After practicing both commands, I understood when to use each one.

---

## Learning Outcome

Today I learned how to navigate the Linux file system, view hidden files, search for files, locate installed commands, and use command history. These commands are useful for working efficiently on Linux servers.

---

## Time Taken

27 minutes

---

## Status

✅ Completed
